# 构建与部署

## RDK 当前调试方式

当前现场调试直接在 RDK 项目目录内修改、构建和启动。旧同步脚本已从 RDK 运行目录清理，避免误执行历史同步流程。

## RDK 构建

```bash
cd /home/sunrise/trash_robot_v3
set +u
source scripts/source_v3.sh
set -u
colcon build
```

## RDK 基础检查

```bash
find scripts -type f | sort
bash -n scripts/*.sh scripts/diagnostics/*.sh scripts/lib/*.sh
python3 -m py_compile scripts/*.py scripts/diagnostics/*.py
./scripts/check.sh
./scripts/diagnostics/check_robot_runtime.sh
```
