# V3 项目缺口检查

当前已按功能模块清理现场入口。后续缺口只围绕当前主链路记录：

1. WebUI 与 Manager/Mission 的资源协调继续收敛。
2. VLM API 模型切换保留在 `config/perception/vlm_provider_registry.yaml`。
3. 本地 VLM 模型当前没有作为运行主线启用；`config/vision/bpu_runtime_policy.yaml` 只是未来预留。
4. 导航、巡逻、抓取必须按 `docs/模块启动说明书.md` 逐模块复测。
